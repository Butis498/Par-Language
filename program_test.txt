program Ejemplo;
var int x,y,z;


main ()
{
read(x);
read(y);
read(z);

if (x < y) then {
    if(x < z) then {
        x = x + 20;
        if (x < z) then {
            z = 50;
        } else {
            write (x);
        }
    }
}
}
