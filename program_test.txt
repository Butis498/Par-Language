program Ejemplo;
var
    int i,j,p;
    int Arreglo[10];
    float valor;
    int Matrix[3][8];
    char f;
    

module int fact (int l,bool f,int gol,char uuu)
var int i,j;
    char k;
    bool o;
{i = j + (p - j*2+j);
read(i);
o = True;
write(j+1);
write(j+1,i);
if (i == 1) then
    {
        if ( 3 +2 == 5) then{ return(i);}
        read(i);
        return (j);}
    else
    {   write(i,j);
        
        return (fact(i,i == 1,i,'f'));}
    
           
}

module void  inicia ()
var int x;
    char l;
{ x=0;
    l = 'l';
    write(i);
    while( x < 11) do
    {
        Arreglo[x] = Arreglo[i+3 * 2] * Matrix[3+1][x+i];
        x = x + 1;
        fact(i,i == 1,i,'f');
    }
    
}

main ()
var
    bool gg;
{ 
    for i = i +1 to 9 + j do{ i = i +1;
        for i = i +1 to 9 + j do{
            write(i);
            write(i);
            write(i);
            read(i);
        }
    }
    
    read(p); j = p *2;
    inicia();

}