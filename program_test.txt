program Ejemplo;
var
    int i,j,p;
    int Arreglo[10];
    float valor;
    int Matrix[3][8];

void module fact (int j)
var int i,j;
    char k;
    bool o;
{i = j + (p - j*2+j);
read(i);
o = True;
write(j+1);
write(j+1,i);
if (j == 1) then
    {
        if ( 3 +2 == 5) then{ return(i);}
        read(i);
        return (j);}
    else
    {   write(i,j);
        return (j* fact(j-1));}
                
}

void module inicia (int y)
var int x;
    char l;
{ x=0;
    l = 'l';
    write(i);
    while( x < 11) do
    {
        Arreglo[x] = i * x;
        x = x + 1;
    }

}

main ()
{ 
    for i = i +1 to 9 + j do{ i = i +1;
        for i = i +1 to 9 + j do{
            write(i);
            write(i);
            write(i);
            read(i);
        }
    }
    read(p); j = p *2;
    inicia( p * j -5);
    

}